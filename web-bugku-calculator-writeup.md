题目名称：计算器
来源：Bugku CTF Web入门题库
标签：Web基础、JS源码审计、前端绕过
难度：入门
题目描述

进入页面随机出现一道加法运算题，需要输入结果点击验证提交答案。
正常交互下输入框仅允许输入一位数字，无法输入正确的两位数答案，无论输入什么数字都会提示输入错误，无法正常通关。

解题思路

题目前端设置了输入长度限制，导致正常做题无法通过校验。
真实的验证逻辑和 Flag 都写在网页的 JS 脚本中，不需要在页面交互做题，直接在开发者工具源代码面板读取专属JS文件，即可找到明文 Flag。

解题详细步骤

1. 打开题目页面，观察页面为随机算式验证功能，尝试手动输入答案，发现始终验证失败。

2. 按下 F12 打开开发者工具，切换到源代码（Sources）面板。

3. 在左侧文件目录找到 js 文件夹，里面存在两个JS文件：jquery公共库、code.js。

4. 排除通用jquery文件，打开本题专属核心脚本 code.js。

5. 阅读代码发现内置判断逻辑：答案正确则弹窗输出 Flag，错误则提示输入有误。

6. Flag 以明文形式直接写在JS源码内，直接复制完整 Flag。

7. 提交 Flag，成功通关题目。

Flag
flag{72ceacc3f9d57bfce47f5d44dc5222c2}
总结与收获

1. 学会区分 Elements元素面板、网页源代码、Sources源码文件 的区别：HTML搭页面骨架，JS控制功能逻辑。

2. 前端的 maxlength 输入限制属于本地浏览器限制，仅限制普通用户，可绕过、不可信。

3. Web入门题常见套路：页面交互不通时，优先查看 Sources 里的 JS 源码，很多Flag直接明文存放。

4. 理解网页运行逻辑：HTML负责展示结构，JS负责运算、判断、弹窗等核心功能。
