题目描述

访问页面，页面源码注释中存在一段假flag，提交无效。提示考点为源码泄露，网站未删除.git版本仓库目录，可下载Git历史快照，找回曾经存在后被删除的文件。

解题思路

1. 网页上直接能看到的flag是陷阱，提交无法通关。

2. 网站存在 .git源码泄露 漏洞：开发上线网站时，没有删除.git文件夹，外网可直接访问。

3. Git会保存全部文件历史快照，就算后续把flag.txt文件删除，旧提交快照中依旧保留该文件。

4. 使用git-dumper工具下载远程Git仓库到本地；利用git reflog查看全部历史提交记录，寻找包含flag的commit快照；使用git show + commit哈希查看指定提交的文件内容，提取真实flag。

解题详细步骤

1. 访问题目页面，查看网页源码，HTML注释内发现假flag，尝试提交验证失败，判断是干扰信息。

2. 判断漏洞：存在.git源码泄露，准备工具。安装Git、Python、git-dumper工具。

3. 查看Git所有历史操作日志，寻找带有flag的commit提交
